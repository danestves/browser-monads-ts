{
  "version": "1.2.5",
  "license": "MIT",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "start": "tsdx watch",
    "build": "tsdx build",
    "test": "tsdx test",
    "lint": "tsdx lint src",
    "prepare": "tsdx build",
    "size": "size-limit",
    "release:major": "changelog -M && git add CHANGELOG.md && git commit -m 'chore(auto): updated CHANGELOG.md' && npm version major && git push origin && git push origin --tags",
    "release:minor": "changelog -m && git add CHANGELOG.md && git commit -m 'chore(auto): updated CHANGELOG.md' && npm version minor && git push origin && git push origin --tags",
    "release:patch": "changelog -p && git add CHANGELOG.md && git commit -m 'chore(auto): updated CHANGELOG.md' && npm version patch && git push origin && git push origin --tags"
  },
  "peerDependencies": {},
  "prettier": {
    "printWidth": 80,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "name": "browser-monads-ts",
  "description": "Provide interfaces for the window, document and navigator variables of a webpage.",
  "author": "Daniel Esteves",
  "module": "dist/browser-monads-ts.esm.js",
  "size-limit": [
    {
      "path": "dist/browser-monads-ts.cjs.production.min.js",
      "limit": "10 KB"
    },
    {
      "path": "dist/browser-monads-ts.esm.js",
      "limit": "10 KB"
    }
  ],
  "devDependencies": {
    "@rollup/plugin-replace": "^3.0.0",
    "@size-limit/preset-small-lib": "^7.0.3",
    "cross-env": "^7.0.3",
    "generate-changelog": "^1.8.0",
    "husky": "^7.0.4",
    "size-limit": "7.0.5",
    "tsdx": "^0.14.1",
    "tslib": "^2.3.1",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "nothing-mock": "^1.0.4"
  },
  "homepage": "https://github.com/danestves/browser-monads-ts#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/danestves/browser-monads-ts.git"
  },
  "bugs": {
    "url": "https://github.com/danestves/browser-monads-ts/issues",
    "email": "estevesd8@gmail.com"
  }
}
